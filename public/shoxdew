<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOX dew | Coding the Neon Future</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <style>
        :root {
            --deep-black: #000000;
            --neon-purple: #8a2be2;
            --electric-blue: #0066ff;
            --dark-bg: #0a0a0f;
            --glass-bg: rgba(15, 15, 25, 0.7);
            --neon-glow: 0 0 10px rgba(138, 43, 226, 0.7), 0 0 20px rgba(138, 43, 226, 0.5), 0 0 30px rgba(138, 43, 226, 0.3);
            --blue-glow: 0 0 10px rgba(0, 102, 255, 0.7), 0 0 20px rgba(0, 102, 255, 0.5), 0 0 30px rgba(0, 102, 255, 0.3);
            --gradient-purple-blue: linear-gradient(90deg, var(--neon-purple), var(--electric-blue));
            --gradient-blue-purple: linear-gradient(90deg, var(--electric-blue), var(--neon-purple));
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Segoe UI', 'Roboto', sans-serif;
            background-color: var(--deep-black);
            color: #e0e0ff;
            overflow-x: hidden;
            line-height: 1.6;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        section {
            padding: 100px 0;
            position: relative;
        }

        /* Общие стили для заголовков */
        .section-title {
            font-size: 2.8rem;
            margin-bottom: 70px;
            text-align: center;
            background: var(--gradient-purple-blue);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: var(--neon-glow);
            position: relative;
            opacity: 0;
            transform: translateY(20px);
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 150px;
            height: 3px;
            background: var(--gradient-purple-blue);
            border-radius: 3px;
            box-shadow: var(--neon-glow);
        }

        /* Стили для стеклянных панелей */
        .glass-panel {
            background: var(--glass-bg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(138, 43, 226, 0.3);
            border-radius: 16px;
            padding: 30px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(30px);
        }

        .glass-panel:hover {
            transform: scale(1.03) translateY(-5px);
            border-color: var(--neon-purple);
            box-shadow: var(--neon-glow), 0 15px 35px rgba(0, 0, 0, 0.5);
        }

        .glass-panel::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-purple-blue);
            z-index: -1;
            border-radius: 18px;
            opacity: 0;
            transition: opacity 0.5s;
        }

        .glass-panel:hover::before {
            opacity: 0.6;
        }

        /* Кнопки с неоновым эффектом */
        .neon-btn {
            display: inline-block;
            padding: 14px 32px;
            background: transparent;
            border: 2px solid var(--neon-purple);
            color: white;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.4s ease;
            text-decoration: none;
            position: relative;
            overflow: hidden;
            z-index: 1;
            letter-spacing: 0.5px;
        }

        .neon-btn:hover {
            background: var(--neon-purple);
            box-shadow: var(--neon-glow);
            transform: translateY(-3px);
        }

        .pulse-btn {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(138, 43, 226, 0.7);
            }
            70% {
                box-shadow: 0 0 0 12px rgba(138, 43, 226, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(138, 43, 226, 0);
            }
        }

        /* Эффект появления элементов */
        .fade-in {
            opacity: 1 !important;
            transform: translateY(0) !important;
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        /* Hero секция */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        #particles-3d {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .hero-content {
            text-align: center;
            z-index: 10;
            max-width: 900px;
            padding: 0 20px;
            opacity: 0;
            transform: translateY(30px);
        }

        .logo {
            font-size: 5.5rem;
            font-weight: 900;
            background: var(--gradient-purple-blue);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: var(--neon-glow);
            margin-bottom: 25px;
            letter-spacing: 3px;
            position: relative;
            display: inline-block;
        }

        .logo::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 10%;
            width: 80%;
            height: 3px;
            background: var(--gradient-purple-blue);
            box-shadow: var(--neon-glow);
            border-radius: 3px;
        }

        .tagline {
            font-size: 2rem;
            margin-bottom: 50px;
            color: #d0d0ff;
            text-shadow: 0 0 15px rgba(0, 102, 255, 0.5);
            font-weight: 300;
            letter-spacing: 1px;
        }

        /* About секция */
        .about-content {
            display: flex;
            align-items: flex-start;
            gap: 60px;
            flex-wrap: wrap;
        }

        .about-text {
            flex: 1;
            min-width: 300px;
            opacity: 0;
            transform: translateX(-30px);
        }

        .about-text p {
            margin-bottom: 25px;
            font-size: 1.15rem;
            color: #c0c0ff;
            line-height: 1.8;
        }

        .neon-divider {
            flex: 0 0 4px;
            min-height: 400px;
            background: var(--gradient-blue-purple);
            border-radius: 4px;
            box-shadow: var(--neon-glow);
            opacity: 0;
            transform: translateY(30px);
        }

        .about-skills {
            flex: 1;
            min-width: 300px;
            opacity: 0;
            transform: translateX(30px);
        }

        .skill-tag {
            display: inline-block;
            padding: 8px 16px;
            margin: 0 10px 15px 0;
            background: rgba(0, 102, 255, 0.1);
            border: 1px solid rgba(0, 102, 255, 0.3);
            border-radius: 8px;
            color: #a0a0ff;
            font-size: 0.95rem;
            transition: all 0.3s ease;
        }

        .skill-tag:hover {
            background: rgba(0, 102, 255, 0.2);
            border-color: var(--electric-blue);
            transform: translateY(-3px);
        }

        /* Skills секции */
        .skills-category {
            margin-bottom: 60px;
        }

        .category-title {
            font-size: 1.8rem;
            margin-bottom: 30px;
            color: var(--neon-purple);
            text-shadow: 0 0 10px rgba(138, 43, 226, 0.5);
            display: flex;
            align-items: center;
        }

        .category-title i {
            margin-right: 15px;
            font-size: 1.5rem;
        }

        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
        }

        .skill-card {
            text-align: center;
            padding: 30px 20px;
        }

        .skill-icon {
            font-size: 3.5rem;
            margin-bottom: 25px;
            color: var(--electric-blue);
            text-shadow: var(--blue-glow);
            transition: all 0.4s ease;
        }

        .skill-card:hover .skill-icon {
            transform: scale(1.2);
            color: var(--neon-purple);
        }

        .skill-card h3 {
            font-size: 1.4rem;
            margin-bottom: 15px;
            color: white;
        }

        .skill-card p {
            color: #b0b0ff;
            font-size: 0.95rem;
        }

        /* Projects секция */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 35px;
        }

        .project-card {
            height: 280px;
            border-radius: 16px;
            overflow: hidden;
            position: relative;
            opacity: 0;
            transform: translateY(40px);
        }

        .project-card::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--gradient-purple-blue);
            z-index: -1;
            border-radius: 18px;
            opacity: 0.7;
        }

        .project-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.7s ease;
            filter: brightness(0.8);
        }

        .project-card:hover .project-image {
            transform: scale(1.08);
            filter: brightness(1);
        }

        .project-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(5px);
            padding: 25px;
            transform: translateY(100%);
            transition: transform 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .project-card:hover .project-overlay {
            transform: translateY(0);
        }

        .project-tech {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 15px;
        }

        .tech-tag {
            padding: 4px 10px;
            background: rgba(138, 43, 226, 0.2);
            border: 1px solid rgba(138, 43, 226, 0.4);
            border-radius: 6px;
            font-size: 0.8rem;
            color: #c0a0ff;
        }

        /* Contact секция */
        .contact-form {
            max-width: 700px;
            margin: 0 auto;
            opacity: 0;
            transform: translateY(30px);
        }

        .form-row {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }

        @media (max-width: 768px) {
            .form-row {
                flex-direction: column;
                gap: 0;
            }
        }

        .form-group {
            flex: 1;
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            color: #d0d0ff;
            font-weight: 500;
        }

        .form-control {
            width: 100%;
            padding: 16px 20px;
            background: rgba(10, 10, 20, 0.7);
            border: 1px solid rgba(138, 43, 226, 0.5);
            border-radius: 10px;
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--neon-purple);
            box-shadow: var(--neon-glow);
            background: rgba(15, 15, 30, 0.9);
        }

        textarea.form-control {
            min-height: 180px;
            resize: vertical;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 60px 0 40px;
            border-top: 1px solid rgba(138, 43, 226, 0.3);
            color: #a0a0ff;
            font-size: 0.95rem;
            position: relative;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin: 30px 0 40px;
        }

        .social-links a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: rgba(138, 43, 226, 0.1);
            border: 1px solid rgba(138, 43, 226, 0.3);
            color: #c0a0ff;
            font-size: 1.3rem;
            transition: all 0.4s ease;
            text-decoration: none;
        }

        .social-links a:hover {
            background: var(--neon-purple);
            color: white;
            transform: translateY(-5px);
            box-shadow: var(--neon-glow);
        }

        .copyright {
            margin-top: 30px;
            color: #8888cc;
        }

        /* Навигация */
        .nav-dots {
            position: fixed;
            right: 30px;
            top: 50%;
            transform: translateY(-50%);
            z-index: 100;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .nav-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: rgba(138, 43, 226, 0.5);
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-dot.active {
            background: var(--neon-purple);
            box-shadow: 0 0 10px rgba(138, 43, 226, 0.8);
            transform: scale(1.3);
        }

        .nav-dot::after {
            content: '';
            position: absolute;
            top: -5px;
            left: -5px;
            right: -5px;
            bottom: -5px;
            border-radius: 50%;
            border: 1px solid rgba(138, 43, 226, 0.3);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .nav-dot:hover::after {
            opacity: 1;
        }

        /* Адаптивность */
        @media (max-width: 1100px) {
            .logo {
                font-size: 4.5rem;
            }
            
            .tagline {
                font-size: 1.7rem;
            }
            
            .section-title {
                font-size: 2.4rem;
            }
            
            .about-content {
                gap: 40px;
            }
        }

        @media (max-width: 900px) {
            .logo {
                font-size: 3.8rem;
            }
            
            .tagline {
                font-size: 1.5rem;
            }
            
            section {
                padding: 80px 0;
            }
            
            .about-content {
                flex-direction: column;
            }
            
            .neon-divider {
                width: 80%;
                height: 4px;
                min-height: auto;
                margin: 30px auto;
            }
            
            .nav-dots {
                display: none;
            }
        }

        @media (max-width: 768px) {
            .logo {
                font-size: 3.2rem;
            }
            
            .tagline {
                font-size: 1.3rem;
                margin-bottom: 40px;
            }
            
            .section-title {
                font-size: 2.2rem;
            }
            
            .skills-grid, .projects-grid {
                grid-template-columns: 1fr;
            }
            
            .glass-panel {
                padding: 25px 20px;
            }
        }

        @media (max-width: 576px) {
            .logo {
                font-size: 2.8rem;
            }
            
            .tagline {
                font-size: 1.2rem;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .hero-content {
                padding: 0 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Навигационные точки -->
    <div class="nav-dots">
        <div class="nav-dot active" data-target="hero"></div>
        <div class="nav-dot" data-target="about"></div>
        <div class="nav-dot" data-target="skills"></div>
        <div class="nav-dot" data-target="projects"></div>
        <div class="nav-dot" data-target="contact"></div>
    </div>

    <!-- Hero секция -->
    <section class="hero" id="hero">
        <canvas id="particles-3d"></canvas>
        <div class="container">
            <div class="hero-content">
                <h1 class="logo">SHOX dew</h1>
                <p class="tagline">Coding the Neon Future.</p>
                <a href="#about" class="neon-btn pulse-btn">Погрузиться в код</a>
            </div>
        </div>
    </section>

    <!-- About секция -->
    <section id="about">
        <div class="container">
            <h2 class="section-title">Обо мне</h2>
            <div class="about-content">
                <div class="about-text glass-panel">
                    <p>Меня зовут Фаридун, мне 19 лет, я занимаюсь программированием и созданием современных приложений. Интересуюсь архитектурой серверов, разработкой API, базами данных, мобильной разработкой и веб-фронтендом.</p>
                    <p>Работаю с языками Java, JavaScript и TypeScript, занимаюсь Android-разработкой, создаю интерфейсы на React и проектирую backend-часть через Node.js и Express.</p>
                    <p>Для баз данных использую PostgreSQL, работаю с SQL-запросами, схемами данных, оптимизацией и структурой хранения. Разбираюсь в Linux, Docker, Git и процессах деплоя.</p>
                    <p>Помимо разработки, остаётся интерес к играм и компьютерной индустрии, особенно PUBG, оптимизации систем и анализу игровой механики.</p>
                </div>
                <div class="neon-divider"></div>
                <div class="about-skills glass-panel">
                    <h3 style="margin-bottom: 25px; color: var(--electric-blue);">Технические навыки</h3>
                    <div>
                        <span class="skill-tag">Java</span>
                        <span class="skill-tag">JavaScript</span>
                        <span class="skill-tag">TypeScript</span>
                        <span class="skill-tag">React</span>
                        <span class="skill-tag">Node.js</span>
                        <span class="skill-tag">Express</span>
                        <span class="skill-tag">PostgreSQL</span>
                        <span class="skill-tag">REST API</span>
                        <span class="skill-tag">Android</span>
                        <span class="skill-tag">Docker</span>
                        <span class="skill-tag">Git</span>
                        <span class="skill-tag">Linux</span>
                        <span class="skill-tag">SQL</span>
                        <span class="skill-tag">Backend Architecture</span>
                        <span class="skill-tag">Deployment</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Skills секция -->
    <section id="skills">
        <div class="container">
            <h2 class="section-title">Навыки</h2>
            
            <!-- Programming Skills -->
            <div class="skills-category">
                <h3 class="category-title"><i class="fas fa-code"></i> Programming & Development</h3>
                <div class="skills-grid">
                    <!-- Java -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-java"></i>
                        </div>
                        <h3>Java</h3>
                        <p>Backend разработка, Android приложения, Spring Framework</p>
                    </div>
                    
                    <!-- JavaScript/TypeScript -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-js-square"></i>
                        </div>
                        <h3>JavaScript / TypeScript</h3>
                        <p>Современный ES6+, строгая типизация, асинхронное программирование</p>
                    </div>
                    
                    <!-- React -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-react"></i>
                        </div>
                        <h3>React</h3>
                        <p>SPA приложения, хуки, управление состоянием, UI компоненты</p>
                    </div>
                    
                    <!-- Node.js -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-node-js"></i>
                        </div>
                        <h3>Node.js + Express</h3>
                        <p>Серверная разработка, REST API, middleware, аутентификация</p>
                    </div>
                    
                    <!-- PostgreSQL -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fas fa-database"></i>
                        </div>
                        <h3>PostgreSQL</h3>
                        <p>DDL/DML, оптимизация запросов, схемы данных, индексы</p>
                    </div>
                    
                    <!-- Docker -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-docker"></i>
                        </div>
                        <h3>Docker</h3>
                        <p>Контейнеризация, оркестрация, деплой приложений</p>
                    </div>
                    
                    <!-- Git -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-git-alt"></i>
                        </div>
                        <h3>Git</h3>
                        <p>Ветвление, workflow, CI/CD, управление версиями</p>
                    </div>
                    
                    <!-- Linux -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fab fa-linux"></i>
                        </div>
                        <h3>Linux</h3>
                        <p>Администрирование серверов, CLI, процессы, bash скрипты</p>
                    </div>
                </div>
            </div>
            
            <!-- Games & Tech Skills -->
            <div class="skills-category">
                <h3 class="category-title"><i class="fas fa-gamepad"></i> Games & Tech</h3>
                <div class="skills-grid">
                    <!-- PUBG -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fas fa-crosshairs"></i>
                        </div>
                        <h3>PUBG</h3>
                        <p>Анализ механик, баллистики, стратегий и геймплея</p>
                    </div>
                    
                    <!-- Game Industry -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fas fa-industry"></i>
                        </div>
                        <h3>Игровая индустрия</h3>
                        <p>Знание современных технологий, трендов и механик</p>
                    </div>
                    
                    <!-- PC Optimization -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fas fa-tachometer-alt"></i>
                        </div>
                        <h3>Настройка систем</h3>
                        <p>Оптимизация ПК, драйверы, железо, производительность</p>
                    </div>
                    
                    <!-- Tech Analysis -->
                    <div class="glass-panel skill-card">
                        <div class="skill-icon">
                            <i class="fas fa-microchip"></i>
                        </div>
                        <h3>Анализ технологий</h3>
                        <p>Изучение новых технологий, сравнение решений, выбор стеков</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Projects секция -->
    <section id="projects">
        <div class="container">
            <h2 class="section-title">Проекты</h2>
            <div class="projects-grid">
                <!-- Проект 1 -->
                <div class="project-card glass-panel">
                    <img src="photo/Снимок экрана 2025-12-07 184755.png" alt="Профиль курьера" class="project-image">
                    <div class="project-overlay">
                        <h3>Приложение для курьеров</h3>
                        <p>Мобильное приложение для управления доставками с системой рейтинга и заработка</p>
                        <div class="project-tech">
                            <span class="tech-tag">Android</span>
                            <span class="tech-tag">Java</span>
                            <span class="tech-tag">REST API</span>
                            <span class="tech-tag">PostgreSQL</span>
                        </div>
                    </div>
                </div>
                
                <!-- Проект 2 -->
                <div class="project-card glass-panel">
                    <div style="width: 100%; height: 100%; background: linear-gradient(45deg, #0a001a, #000033); display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-server" style="font-size: 4rem; color: var(--neon-purple);"></i>
                    </div>
                    <div class="project-overlay">
                        <h3>Backend API система</h3>
                        <p>Масштабируемый REST API для управления пользователями и контентом</p>
                        <div class="project-tech">
                            <span class="tech-tag">Node.js</span>
                            <span class="tech-tag">Express</span>
                            <span class="tech-tag">PostgreSQL</span>
                            <span class="tech-tag">JWT</span>
                            <span class="tech-tag">Docker</span>
                        </div>
                    </div>
                </div>
                
                <!-- Проект 3 -->
                <div class="project-card glass-panel">
                    <div style="width: 100%; height: 100%; background: linear-gradient(45deg, #000033, #001a33); display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-globe" style="font-size: 4rem; color: var(--electric-blue);"></i>
                    </div>
                    <div class="project-overlay">
                        <h3>Веб-платформа</h3>
                        <p>React SPA с современным UI, аутентификацией и интеграцией с API</p>
                        <div class="project-tech">
                            <span class="tech-tag">React</span>
                            <span class="tech-tag">TypeScript</span>
                            <span class="tech-tag">SCSS</span>
                            <span class="tech-tag">REST API</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact секция -->
    <section id="contact">
        <div class="container">
            <h2 class="section-title">Контакты</h2>
            <form class="contact-form glass-panel">
                <div class="form-row">
                    <div class="form-group">
                        <label for="name">Имя</label>
                        <input type="text" id="name" class="form-control" placeholder="Ваше имя" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" class="form-control" placeholder="Ваш email" required>
                    </div>
                </div>
                <div class="form-group">
                    <label for="subject">Тема</label>
                    <input type="text" id="subject" class="form-control" placeholder="Тема сообщения" required>
                </div>
                <div class="form-group">
                    <label for="message">Сообщение</label>
                    <textarea id="message" class="form-control" placeholder="Ваше сообщение..." required></textarea>
                </div>
                <button type="submit" class="neon-btn pulse-btn" style="width: 100%; padding: 16px; font-size: 1.2rem;">
                    Отправить сообщение <i class="fas fa-paper-plane" style="margin-left: 10px;"></i>
                </button>
            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <h2 class="logo" style="font-size: 3rem; margin-bottom: 20px;">SHOX dew</h2>
            <p style="font-size: 1.2rem; margin-bottom: 30px; color: #d0d0ff;">Coding the Neon Future.</p>
            
            <div class="social-links">
                <a href="#"><i class="fab fa-github"></i></a>
                <a href="#"><i class="fab fa-telegram"></i></a>
                <a href="#"><i class="fab fa-linkedin-in"></i></a>
                <a href="#"><i class="fab fa-codepen"></i></a>
                <a href="#"><i class="fas fa-envelope"></i></a>
            </div>
            
            <p class="copyright">© 2023 SHOX dew. Все права защищены.<br>Разработано с <i class="fas fa-heart" style="color: var(--neon-purple);"></i> и неоновым светом.</p>
        </div>
    </footer>

    <script>
        // Инициализация Three.js для 3D фона
        function initParticles() {
            const canvas = document.getElementById('particles-3d');
            const scene = new THREE.Scene();
            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });
            
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
            
            // Создаем частицы
            const particleGeometry = new THREE.BufferGeometry();
            const particleCount = 2000;
            
            const positions = new Float32Array(particleCount * 3);
            const colors = new Float32Array(particleCount * 3);
            
            for(let i = 0; i < particleCount * 3; i += 3) {
                // Позиции
                positions[i] = (Math.random() - 0.5) * 30;
                positions[i + 1] = (Math.random() - 0.5) * 30;
                positions[i + 2] = (Math.random() - 0.5) * 30;
                
                // Цвета (неоновые оттенки)
                colors[i] = Math.random() * 0.5 + 0.5;     // R
                colors[i + 1] = Math.random() * 0.3 + 0.3; // G
                colors[i + 2] = Math.random() * 0.8 + 0.2; // B
            }
            
            particleGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
            particleGeometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));
            
            const particleMaterial = new THREE.PointsMaterial({
                size: 0.05,
                vertexColors: true,
                transparent: true,
                opacity: 0.8,
                blending: THREE.AdditiveBlending
            });
            
            const particles = new THREE.Points(particleGeometry, particleMaterial);
            scene.add(particles);
            
            // Создаем неоновые линии
            const lineGeometry = new THREE.BufferGeometry();
            const lineCount = 50;
            const linePositions = new Float32Array(lineCount * 6);
            
            for(let i = 0; i < lineCount * 6; i += 6) {
                const startX = (Math.random() - 0.5) * 30;
                const startY = (Math.random() - 0.5) * 30;
                const startZ = (Math.random() - 0.5) * 30;
                
                const endX = startX + (Math.random() - 0.5) * 10;
                const endY = startY + (Math.random() - 0.5) * 10;
                const endZ = startZ + (Math.random() - 0.5) * 10;
                
                linePositions[i] = startX;
                linePositions[i + 1] = startY;
                linePositions[i + 2] = startZ;
                linePositions[i + 3] = endX;
                linePositions[i + 4] = endY;
                linePositions[i + 5] = endZ;
            }
            
            lineGeometry.setAttribute('position', new THREE.BufferAttribute(linePositions, 3));
            
            const lineMaterial = new THREE.LineBasicMaterial({
                color: 0x8a2be2,
                transparent: true,
                opacity: 0.3,
                blending: THREE.AdditiveBlending
            });
            
            const lines = new THREE.LineSegments(lineGeometry, lineMaterial);
            scene.add(lines);
            
            camera.position.z = 15;
            
            // Анимация
            function animate() {
                requestAnimationFrame(animate);
                
                particles.rotation.x += 0.0005;
                particles.rotation.y += 0.001;
                
                lines.rotation.x += 0.0003;
                lines.rotation.y += 0.0007;
                
                renderer.render(scene, camera);
            }
            
            animate();
            
            // Обработка изменения размера окна
            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });
        }
        
        // Инициализация GSAP анимаций
        document.addEventListener('DOMContentLoaded', function() {
            // Инициализация 3D фона
            initParticles();
            
            // Инициализация ScrollTrigger
            gsap.registerPlugin(ScrollTrigger);
            
            // Анимация появления героя
            gsap.to('.hero-content', {
                duration: 1.8,
                opacity: 1,
                y: 0,
                ease: "power3.out",
                delay: 0.5
            });
            
            // Анимация для секций при скролле
            const animatedElements = document.querySelectorAll('.section-title, .glass-panel, .neon-divider');
            
            animatedElements.forEach(element => {
                gsap.to(element, {
                    opacity: 1,
                    y: 0,
                    x: 0,
                    duration: 1.2,
                    ease: "power3.out",
                    scrollTrigger: {
                        trigger: element,
                        start: "top 85%",
                        toggleActions: "play none none reverse"
                    }
                });
            });
            
            // Анимация для навигационных точек
            const sections = document.querySelectorAll('section');
            const navDots = document.querySelectorAll('.nav-dot');
            
            // Обновление активной точки при скролле
            ScrollTrigger.create({
                trigger: 'body',
                start: 'top top',
                end: 'bottom bottom',
                onUpdate: (self) => {
                    const scrollPos = self.scroll();
                    
                    sections.forEach((section, index) => {
                        const sectionTop = section.offsetTop;
                        const sectionHeight = section.offsetHeight;
                        
                        if (scrollPos >= sectionTop - 200 && scrollPos < sectionTop + sectionHeight - 200) {
                            navDots.forEach(dot => dot.classList.remove('active'));
                            navDots[index].classList.add('active');
                        }
                    });
                }
            });
            
            // Клик по навигационным точкам
            navDots.forEach((dot, index) => {
                dot.addEventListener('click', () => {
                    const targetSection = sections[index];
                    window.scrollTo({
                        top: targetSection.offsetTop,
                        behavior: 'smooth'
                    });
                });
            });
            
            // Анимация при наведении на карточки навыков
            const skillCards = document.querySelectorAll('.skill-card');
            skillCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    gsap.to(this, {
                        scale: 1.05,
                        duration: 0.4,
                        ease: "back.out(1.7)"
                    });
                });
                
                card.addEventListener('mouseleave', function() {
                    gsap.to(this, {
                        scale: 1,
                        duration: 0.4,
                        ease: "power2.out"
                    });
                });
            });
            
            // Эффект для кнопки отправки формы
            const submitBtn = document.querySelector('#contact .neon-btn');
            submitBtn.addEventListener('click', function(e) {
                e.preventDefault();
                
                // Эффект электрического импульса
                const pulse = document.createElement('div');
                pulse.style.position = 'absolute';
                pulse.style.top = '0';
                pulse.style.left = '0';
                pulse.style.width = '100%';
                pulse.style.height = '100%';
                pulse.style.background = 'radial-gradient(circle, rgba(138,43,226,0.9) 0%, rgba(0,102,255,0.5) 50%, transparent 70%)';
                pulse.style.borderRadius = '10px';
                pulse.style.zIndex = '-1';
                pulse.style.opacity = '0';
                
                this.appendChild(pulse);
                
                // Анимация импульса
                gsap.to(pulse, {
                    opacity: 1,
                    scale: 1.5,
                    duration: 0.5,
                    ease: "power2.out",
                    onComplete: () => {
                        pulse.remove();
                        // Показываем уведомление
                        alert('Сообщение отправлено! Спасибо за ваше сообщение. Я свяжусь с вами в ближайшее время.');
                        
                        // Очищаем форму
                        document.getElementById('name').value = '';
                        document.getElementById('email').value = '';
                        document.getElementById('subject').value = '';
                        document.getElementById('message').value = '';
                    }
                });
            });
            
            // Параллакс эффект для фона
            gsap.to('#particles-3d', {
                y: '30%',
                ease: 'none',
                scrollTrigger: {
                    trigger: '.hero',
                    start: 'top top',
                    end: 'bottom top',
                    scrub: true
                }
            });
            
            // Анимация неонового свечения для заголовков
            const titles = document.querySelectorAll('.section-title, .category-title, .logo');
            titles.forEach(title => {
                setInterval(() => {
                    gsap.to(title, {
                        textShadow: '0 0 15px rgba(138, 43, 226, 0.9), 0 0 25px rgba(138, 43, 226, 0.7), 0 0 35px rgba(138, 43, 226, 0.5)',
                        duration: 1.5,
                        yoyo: true,
                        repeat: 1,
                        ease: "sine.inOut"
                    });
                }, 5000);
            });
        });
    </script>
</body>
</html>
